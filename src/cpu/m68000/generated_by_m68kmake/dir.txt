This directory contains the source files generated by m68kmake.exe.

These files are copied from a MinGW GCC build run and the include statements for
"m68kcpu.h" are changed to point to the parent directory ("../m68kcpu.h")

TODO: Handle this dynamically in MSVC as it is done with Make/GCC
